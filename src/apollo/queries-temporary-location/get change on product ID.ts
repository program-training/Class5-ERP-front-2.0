import { gql } from "@apollo/client";

export const GET_CHANGE = gql`
  query nameQuery {
    nameQuery {
      name
      returns
      name
      returns
      name
      returns
      name
      returns
    }
  }
`;
